import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: "Arcane Terminal Blog",
  description: "A modern Arch Linux-inspired full-stack blog built with Next.js, Supabase, Tailwind, and Tiptap.",
  metadataBase: new URL("https://your-domain.vercel.app"),
  openGraph: {
    title: "Arcane Terminal Blog",
    description: "A futuristic Linux-inspired blog with rich articles, syntax highlighting, and Supabase-powered admin.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
